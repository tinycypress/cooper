// :tile keyword should be reserves for all tiles not just tiles with a base on as currently used.
// TilesRouter.get('/:tile', getTile);

// import { Router } from "express";
// import TileHelper from "../../../operations/minigames/medium/conquest/baseHelper";

// const TilesRouter = Router();


// export const getTiles = async (req, res) => res.status(200).json(await TileHelper.all());
// TilesRouter.get('/', getTiles);

// export const getTile = async (req, res) => res.status(200).json(await TileHelper.get(req.params.base));
// TilesRouter.get('/:base', getTile);
// TilesRouter.get('/base/:base', getTile);



// export default TilesRouter;

